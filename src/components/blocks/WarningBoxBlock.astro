---
import { Icon } from 'astro-icon/components';
import { marked } from 'marked';

interface Props {
  content: string;
}

const { content } = Astro.props;

// Parse markdown pokud není HTML
const isHtml = content.includes('<p>') || content.includes('<h2>');
const htmlContent = isHtml ? content : marked.parse(content);
---

<div class="bg-amber-50 border-l-4 border-amber-500 rounded-lg p-6 mb-8">
  <div class="flex items-start gap-4">
    <div class="shrink-0">
      <Icon name="mdi:alert" class="w-6 h-6 text-amber-600" />
    </div>
    <div class="flex-1">
      <div class="prose prose-amber max-w-none text-amber-900">
        <Fragment set:html={htmlContent} />
      </div>
    </div>
  </div>
</div>
