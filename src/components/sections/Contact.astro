---
import { Icon } from 'astro-icon/components';
import { contactInfo } from '../../data/contact';
---

<section id="kontakt" class="py-12 md:py-20 relative overflow-hidden">
  <!-- Gradient Background -->
  <div class="absolute inset-0 bg-linear-to-br from-blue-600 via-cyan-600 to-blue-700"></div>
  
  <div class="container mx-auto px-4 relative z-10">
    <div class="max-w-4xl mx-auto">
      <h2 class="text-2xl md:text-4xl font-bold mb-3 md:mb-4 text-center text-white">Kde nás najdete</h2>
      <p class="text-center text-blue-100 mb-8 md:mb-12 text-base md:text-lg">Jsme tu pro vás a vaše děti</p>
      <div class="grid md:grid-cols-2 gap-6 md:gap-8">
        <div class="space-y-5 md:space-y-6 text-white">
          <div>
            <h3 class="font-semibold text-lg md:text-xl mb-3 md:mb-4">Ordinace Zdravíčko</h3>
            <div class="space-y-3">
              <div class="flex items-start gap-3">
                <Icon name="mdi:map-marker" class="w-5 h-5 md:w-6 md:h-6 shrink-0 mt-1" />
                <div class="text-sm md:text-base">
                  <p>{contactInfo.address.street}</p>
                  <p>{contactInfo.address.zip} {contactInfo.address.city}</p>
                </div>
              </div>
              <div class="flex items-start gap-3">
                <Icon name="mdi:phone" class="w-6 h-6 shrink-0 mt-1" />
                <div>
                  <p><a href={`tel:${contactInfo.phones.doctor.number}`} class="hover:underline">{contactInfo.phones.doctor.display}</a> ({contactInfo.phones.doctor.label})</p>
                  <p><a href={`tel:${contactInfo.phones.nurse.number}`} class="hover:underline">{contactInfo.phones.nurse.display}</a> ({contactInfo.phones.nurse.label})</p>
                </div>
              </div>
              <div class="flex items-center gap-3">
                <Icon name="mdi:email" class="w-6 h-6 shrink-0" />
                <a href={`mailto:${contactInfo.email}`} class="hover:underline">{contactInfo.email}</a>
              </div>
            </div>
          </div>
          <div>
            <h4 class="font-semibold mb-2">Pro objednání</h4>
            <p class="text-blue-100">Zavolejte nám nebo napište email. Rádi vám najdeme vhodný termín.</p>
          </div>
        </div>
        <div class="bg-white rounded-xl p-6 shadow-lg">
          <h3 class="font-semibold text-xl mb-4 text-gray-900">Rychlý kontakt</h3>
          <div class="space-y-4">
            <p class="text-gray-600 mb-6">
              Máte dotaz nebo chcete se objednat? Kontaktujte nás telefonicky, emailem nebo přes kontaktní formulář.
            </p>
            
            <a 
              href="/kontakt"
              class="block w-full bg-blue-600 text-white font-semibold px-6 py-4 rounded-lg hover:bg-blue-700 transition-colors shadow-md text-center"
            >
              <Icon name="mdi:email-edit" class="w-5 h-5 inline-block mr-2" />
              Kontaktní formulář
            </a>
            
            <div class="pt-4 border-t-2 border-gray-200">
              <p class="text-sm text-gray-600 text-center mb-3">Nebo zavolejte přímo</p>
              <a 
                href={`tel:${contactInfo.phones.doctor.number}`}
                class="block w-full bg-gray-100 text-gray-900 font-semibold px-6 py-3 rounded-lg hover:bg-gray-200 transition-colors text-center"
              >
                <Icon name="mdi:phone" class="w-5 h-5 inline-block mr-2" />
                {contactInfo.phones.doctor.display}
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
