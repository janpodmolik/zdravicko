---
/**
 * Hero sekce s profesionální Lottie animací mráčků
 * Používá hotovou animaci z LottieFiles pro profesionální výsledek
 */

export interface Props {
  /** Hlavní nadpis */
  title?: string;
  /** Popisek pod nadpisem */
  description?: string;
  /** Zobrazit CTA tlačítka */
  showButtons?: boolean;
}

const {
  title = 'Péče o vaše děti s láskou a profesionalitou',
  description = 'Moderní dětská ordinace v 🩵 města. Poskytujeme komplexní zdravotní péči pro děti od narození.',
  showButtons = true,
} = Astro.props;
---

<section class="relative py-12 md:py-20" style="background: linear-gradient(135deg, #3a6fa8 0%, #5085c6 25%, #6ba3e0 50%, #5085c6 75%, #4070b0 100%); overflow: visible;">
  <!-- Lottie Cloud Animation Background - Multiple layers evenly distributed -->

  <!-- Layer 1 - Left side, top -->
  <div class="absolute z-0 pointer-events-none clouds-container" style="transform: translate(-40%, -20%);">
    <dotlottie-player
      src="/animations/clouds.json"
      background="transparent"
      speed="0.12"
      class="clouds-player"
      loop
      autoplay
      intermission="0"
    ></dotlottie-player>
  </div>

  <!-- Layer 2 - Left center, bottom -->
  <div class="absolute z-0 pointer-events-none clouds-container" style="transform: translate(-20%, 25%);">
    <dotlottie-player
      src="/animations/clouds.json"
      background="transparent"
      speed="0.15"
      class="clouds-player"
      loop
      autoplay
      intermission="0"
    ></dotlottie-player>
  </div>

  <!-- Layer 3 - Center, middle -->
  <div class="absolute z-0 pointer-events-none clouds-container" style="transform: translate(0%, 0%);">
    <dotlottie-player
      src="/animations/clouds.json"
      background="transparent"
      speed="0.1"
      class="clouds-player"
      loop
      autoplay
      intermission="0"
    ></dotlottie-player>
  </div>

  <!-- Layer 4 - Right center, bottom -->
  <div class="absolute z-0 pointer-events-none clouds-container" style="transform: translate(20%, 20%);">
    <dotlottie-player
      src="/animations/clouds.json"
      background="transparent"
      speed="0.18"
      class="clouds-player"
      loop
      autoplay
      intermission="0"
    ></dotlottie-player>
  </div>

  <!-- Layer 5 - Right side, top -->
  <div class="absolute z-0 pointer-events-none clouds-container" style="transform: translate(40%, -15%);">
    <dotlottie-player
      src="/animations/clouds.json"
      background="transparent"
      speed="0.13"
      class="clouds-player"
      loop
      autoplay
      intermission="0"
    ></dotlottie-player>
  </div>

  <!-- Layer 6 - Extra far left, middle-top -->
  <div class="absolute z-0 pointer-events-none clouds-container" style="transform: translate(-60%, -5%);">
    <dotlottie-player
      src="/animations/clouds.json"
      background="transparent"
      speed="0.14"
      class="clouds-player"
      loop
      autoplay
      intermission="0"
    ></dotlottie-player>
  </div>

  <!-- Layer 7 - Extra center-left, upper -->
  <div class="absolute z-0 pointer-events-none clouds-container" style="transform: translate(-30%, -25%);">
    <dotlottie-player
      src="/animations/clouds.json"
      background="transparent"
      speed="0.11"
      class="clouds-player"
      loop
      autoplay
      intermission="0"
    ></dotlottie-player>
  </div>

  <!-- Layer 8 - Extra center-right, upper -->
  <div class="absolute z-0 pointer-events-none clouds-container" style="transform: translate(30%, -18%);">
    <dotlottie-player
      src="/animations/clouds.json"
      background="transparent"
      speed="0.16"
      class="clouds-player"
      loop
      autoplay
      intermission="0"
    ></dotlottie-player>
  </div>

  <!-- Content -->
  <div class="container mx-auto px-4 relative z-10">
    <div class="max-w-4xl mx-auto text-center">
      <!-- Semi-transparent background for better text readability -->
      <div class="bg-blue-900/25 rounded-3xl p-8 md:p-12 shadow-2xl" style="backdrop-filter: blur(2px);">
        <h1 class="text-3xl md:text-5xl lg:text-6xl font-bold text-white mb-4 md:mb-6" style="text-shadow: 0 2px 10px rgba(0,0,0,0.4), 0 4px 20px rgba(0,0,0,0.3);">
          {title}
        </h1>
        <p class="text-base md:text-xl text-white/95 mb-6 md:mb-8" style="text-shadow: 0 1px 5px rgba(0,0,0,0.4), 0 2px 10px rgba(0,0,0,0.3);">
          {description}
        </p>
        {showButtons && (
          <div class="flex flex-col sm:flex-row gap-3 md:gap-4 justify-center">
            <a
              href="/o-nas"
              class="bg-white/95 backdrop-blur-sm text-blue-600 px-6 py-3 md:px-8 md:py-4 rounded-lg font-semibold hover:bg-white transition-all hover:scale-105 text-base md:text-lg border-2 border-white shadow-lg"
            >
              Více o nás
            </a>
            <a
              href="/kontakt"
              class="bg-white/20 backdrop-blur-sm text-white px-6 py-3 md:px-8 md:py-4 rounded-lg font-semibold hover:bg-white/30 transition-all hover:scale-105 text-base md:text-lg border-2 border-white/60 shadow-lg"
            >
              Kontakt
            </a>
          </div>
        )}
      </div>
    </div>
  </div>

  <!-- Dekorativní gradient overlay pro hloubku -->
  <div class="absolute inset-0 bg-linear-to-t from-blue-500/20 via-transparent to-transparent pointer-events-none z-2"></div>
</section>

<script>
  // Import Lottie player pro client-side
  import '@dotlottie/player-component';
</script>

<style>
  /* Text shadow pro lepší čitelnost */
  h1, p {
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.15);
  }

  /* Hover efekt pro tlačítka */
  a {
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* Container pro mráčky - roztáhne se za okraje */
  .clouds-container {
    top: 0;
    left: -50%;
    width: 200%;
    height: 100%;
    overflow: visible;
  }

  /* Lottie player - roztažený na celou šířku */
  .clouds-player {
    display: block;
    width: 100% !important;
    height: 100% !important;
    object-fit: cover;
  }
</style>
