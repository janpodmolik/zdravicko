[build]
  command = "npm run build"
  publish = "dist"

# Netlify Forms konfigurace
[[plugins]]
  package = "@netlify/plugin-forms"

# Přesměrování pro admin panel (CMS)
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
